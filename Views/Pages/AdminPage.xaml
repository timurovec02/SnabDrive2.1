<Page x:Class="SnabDrive2._0.Views.Pages.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SnabDrive2._0.Views.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0"
              Margin="5 5">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center">
                
                <Button x:Name="UsersPage"
                        Content="Пользователи"
                        Click="UsersPage_Click"
                        Style="{DynamicResource btnArch}"/>
                
                <Button x:Name="RegeditSettingsPage"
                        Content="Настройка регистра"
                        Click="RegeditSettingsPage_Click"
                        Style="{DynamicResource btnDel}"/>
                
            </StackPanel>

            <Button x:Name="Back"
                    Grid.ColumnSpan="2"
                    Content="Назад"
                    HorizontalAlignment="Right"
                    Click="Back_Click"
                    Style="{DynamicResource btnArch}"/>

        </Grid>
        <Frame x:Name="AdminFrame"
               Grid.Row="1" 
               Content="Выберите, что вам нужно"
               NavigationUIVisibility="Hidden"
               HorizontalContentAlignment="Center"
               VerticalContentAlignment="Center"
               />
    </Grid>
</Page>
